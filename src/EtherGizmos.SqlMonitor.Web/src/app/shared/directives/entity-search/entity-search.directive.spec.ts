import { EntitySearchDirective } from './entity-search.directive';

describe('EntitySearchDirective', () => {
  it('should create an instance', () => {
    const directive = new EntitySearchDirective();
    expect(directive).toBeTruthy();
  });
});
